// Assigment
var height = Number(prompt ("what is the height") );
var widht = Number(prompt ("what is the widht"));
alert ( height * widht )
var perimeter = ( 2 * height + widht);
alert ( "the perameter is " + perimeter);

// Calculate the volume and surface Area of a box using javascript
var height = Number(prompt("What is the Height of cuboid"))
var widht = Number(prompt("What is the width of cuboid"))
var length = Number (prompt("What is the Length of cuboid"))
alert ( "Volume of Cuboid is " + height * widht * length )
alert ("surface area of Cuboid is " + length * widht + widht * height + height * length)
